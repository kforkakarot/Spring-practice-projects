# use wildcard "#" to expose all endpoints
# can also expose individual endpoints using comma-delimited list
#management.endpoints.web.exposure.include=*
#management.info.env.enabled=true

#exclude individual enpoints via a comma-delimited list
#management.endpoints.web.exposure.exclude=info,health
#
#info.app.name=My super cool app
#info.app.description=A carzy fun app, YASOP
#info.app.version=1.0.0

# adding custom values for using @Value annotation for
coach.name=Mickey Mouse
team.name=The Mouse Club

# change spring boot embedded server port

server.port=7070

# all request should be prefixed with mycoolapp

server.servlet.context-path=/mycoolapp